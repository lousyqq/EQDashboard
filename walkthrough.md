# 🎉 多國語言 (i18n) 實作完成

我已經成功為系統全面導入了多國語言支援！現在系統可以完美在 **繁體中文**、**English** 與 **日本語** 之間切換。

## 🌟 主要更新內容

### 1. 翻譯字典擴充 (`config.js`)
我將原本只有零星幾個單字的 `i18n` 翻譯表，擴充到了包含 **100 個以上** 的系統單字與句子。
包含了：
- 登入畫面的所有欄位
- 上方導覽列的所有按鈕與下拉選單
- 側邊欄標題
- 9 大管理頁面的標題與所有**表格的表頭 (Table Headers)**
- 所有新增 / 編輯彈出視窗 (Modal) 裡面的欄位與標籤

### 2. UI 標籤綁定 (`index.html`)
透過腳本精準掃描，我已經將 `index.html` 裡面超過 **100 處** 的靜態中文字，全部打上 `<span data-i18n="...">` 的標籤。
現在只要您從右上角切換語言，整個畫面的文字會**瞬間替換**。

### 3. 廠區預設語言連動
這部分的邏輯底層已經內建：
當您從右上角的「廠區」選單，切換到另一個廠區時，系統會去讀取該廠區在資料庫設定的 `DefaultLang` (例如日文)，然後**自動觸發語言切換**，將整個系統介面變成該廠區的預設語言！

---

## 🔍 如何測試？

1. **登入系統**
2. 點擊右上角的地球圖示 **(繁中)**，切換為 **English** 或 **日本語**。
3. 觀察側邊欄、左上角標題，以及點開任何一個管理頁面（例如「廠區管理」），您會發現表格的表頭、按鈕文字都已經變成了對應的語言。
4. **廠區連動測試**：您可以試著到「廠區管理」把某個廠區的「預設語言」改成日本語，接著去右上角切換到該廠區，看看介面是否自動變成了日文。

> [!NOTE]
> **保留項目說明：**
> 1. 您所建立的看板名稱（例如 `12A_機況_Main`）屬於動態資料，不受此語言切換影響，會維持您原本輸入的名稱。
> 2. DataTables 內建的套件文字（如右下角的 "Previous / Next"）本次為了效能考量並未強制重新渲染，重點已放在您最關心的表頭與主要 UI 版面上。
